import test from 'ava';

import { createRouter } from '../dist';

test.todo('router should have an asObservable method');

test.todo('router.route should match paths from history')

test.todo('rotuer.route onNext callback should flat map to a promise')

test.todo('router.route stream option should be flat mapped to router obsrvable');

test.todo('router.route subroute callback should get a router as an argument');

test.todo('router.route subrouter should match paths joined from parant route');
